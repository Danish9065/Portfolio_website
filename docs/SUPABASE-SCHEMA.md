# Supabase Schema

Run:

1. `supabase/migrations/001_initial_schema.sql`
2. `supabase/seed.sql`

Seed content is demo-only and must be edited before publication. Admin users are created through Supabase Auth, not through a custom password table.
